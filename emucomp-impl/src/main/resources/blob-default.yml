blobstore:
  gc_interval: ${BLOBSTORE_GC_INTERVAL:30 min}
  max_entry_age: ${BLOBSTORE_MAX_ENTRY_AGE:1 hour}
  backend:
    type: ${BLOBSTORE_BACKEND_TYPE:fs}
    base_dir: ${BLOBSTORE_BACKEND_BASE_DIR:/tmp/blobstore}
